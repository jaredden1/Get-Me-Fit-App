<!DOCTYPE html>
<html>

  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.1/css/all.min.css" />
  </head>

  <body>
    <% if (user) { %> <% if (title !== 'Get Me Fit') { %>
    <nav>
      <% if (title === 'Cardio List') { %>
      <a href="/">Start New Workout</a>
      <a href="/logout">LOG OUT</a>
      <% } else if (title === 'Workout List') { %>
      <a href="/">Start New Workout</a>
      <a href="/logout">LOG OUT</a>
      <% } else { %>
      <a href="/">Start New Workout</a>
      <a href="/cardio/new">Add Cardio Workout</a>
      <a href="/workouts/new">Add Weight Training</a>
      <a href="/logout">LOG OUT</a>
      <% } %>
    </nav>

    <% } %> <% } else { %>
      <a href="/auth/google" class="login">LOG IN&nbsp;<img src="https://i.imgur.com/FHjYyi0.png"/></a>

    <% } %> <% if (user && title === 'Workout List') { %>
    <a href="/workouts/new" class="weight-training-btn">Add Weight Training</a>

    <% } %> <% if (user && title === 'Cardio List') { %>
    <a href="/cardio/new" class="cardio-training-btn">Add Cardio</a>

    <% } %>